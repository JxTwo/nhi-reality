app = 'nhi-reality'
primary_region = 'lax'
console_command = '/code/manage.py shell'

[build]

[deploy]
  release_command = "sh -lc 'python manage.py migrate --noinput'"

[env]
  DJANGO_ENV = 'production'
  DJANGO_SETTINGS_MODULE = 'core.settings'
  PORT = '8000'

[processes]
  app = 'gunicorn --bind :8000 --workers 2 core.wsgi'
  celery = 'celery -A core worker -l info -B'

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1

[[statics]]
  guest_path = '/code/static'
  url_prefix = '/static/'
